# Required
version: 2

# Set the OS, Python version and other tools you might need.
build:
  os: ubuntu-24.04
  tools:
    python: "3.13"

sphinx:
  configuration: docs/zh-cn/source/conf.py

python:
    install:
    - requirements: ./requirements.txt